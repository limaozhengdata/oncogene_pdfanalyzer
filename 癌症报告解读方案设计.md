# 癌症检测报告LLM解读方案设计

## 一、项目背景与目标

针对肿瘤检测报告（MRD、PD-L1、HER2等）中的专项指标，设计一套基于LLM的自动化解读流程。核心诉求：

1. 报告类型多样，一份PDF通常只聚焦1~2个指标，需先判断"这是不是一份关于某指标的报告"
2. 只有判断为"是"的指标才进入解读环节，避免无关指标产生幻觉解读
3. 报告关键信息集中在前几页（首页摘要、检测结果页），无需处理全文，控制成本与延迟

整体流程：**PDF关键页文本提取 → 第一步：报告类型/指标存在性判断 → 第二步：仅对判断为"是"的指标循环/批量解读**

---

## 二、配置文件设计

在原有基础上增加别名、关键字段等信息，提升第一步判断的准确率。

```json
{
  "MRD": {
    "aliases": ["MRD", "微小残留病灶", "分子残留病灶", "ctDNA MRD"],
    "description": "用于判断肿瘤治疗后是否存在微量残留病灶，指导辅助治疗决策",
    "key_fields": ["MRD状态", "检测灵敏度", "VAF", "采样时间点"],
    "priority": "high"
  },
  "PDL1": {
    "aliases": ["PD-L1", "PDL1", "程序性死亡受体配体1"],
    "description": "免疫治疗生物标志物，关注TPS/CPS评分及判读标准",
    "key_fields": ["TPS", "CPS", "染色克隆号", "判读结果"],
    "priority": "high"
  },
  "HER2": {
    "aliases": ["HER2", "HER-2", "人表皮生长因子受体2", "ERBB2"],
    "description": "乳腺癌/胃癌靶向治疗关键标志物，关注IHC分级与FISH结果",
    "key_fields": ["IHC结果", "FISH结果", "基因拷贝数"],
    "priority": "high"
  }
}
```

字段说明：

| 字段 | 作用 |
|---|---|
| `aliases` | 覆盖同义词/缩写/中英文写法，避免因命名差异导致第一步漏判 |
| `description` | 提供背景知识，辅助LLM理解该指标的临床意义 |
| `key_fields` | 告知第二步LLM需要提取哪些具体字段，避免解读遗漏或跑偏 |
| `priority` | 用于控制解读顺序或篇幅分配（可选） |

---

## 三、PDF文本提取策略

**结论：不做全文提取，只取关键页/关键区间。**

- 报告结构通常为：封面/患者信息 → 检测结果摘要（关键） → 详细图表/方法学说明 → 附录
- 建议提取策略：
  1. 优先提取 **前3~5页** 的文本（覆盖率高，成本低）
  2. 若前几页中未检测到任何配置指标的关键词（含aliases），再追加提取中间部分（如第6~10页）作为兜底，避免因排版差异漏判
  3. 单指标报告（如本项目定位）中，结果一般在前2页内即可覆盖，实际项目中可先用少量样本报告测试确定合理页数

- 提取时保留基本的段落/表格结构（不要压缩成一整段纯文本），有助于LLM定位数值型结果

---

## 四、第一步：报告类型/指标判断

### 目的

判断"这份报告是否属于某个配置指标的检测报告"，即**报告类型判断**，而非单纯判断"文本里提没提到这个词"。例如报告正文中提及"既往HER2阴性"这类背景描述，不代表这是一份HER2检测报告，不应判定为"是"。

### 输入

前几页提取文本（见第三节）

### 判断逻辑

一份报告可能同时是多个指标的联合报告，因此对配置文件中每个指标独立判断，判断依据：

- 该指标是否有**明确的本次检测结果**（而非仅在病史/既往信息中被提及）
- 该指标是否有对应的**检测方法学说明**（如"采用IHC法检测HER2"）

### 输出格式

```json
{
  "MRD": {"is_report_type": "yes", "status": "已出结果", "evidence": "报告摘要页：MRD检测结果为阳性..."},
  "PDL1": {"is_report_type": "no", "status": "仅病史提及", "evidence": "既往病理提及PD-L1阴性，非本次检测项目"},
  "HER2": {"is_report_type": "yes", "status": "已出结果", "evidence": "HER2 IHC 3+..."}
}
```

字段说明：

- `is_report_type`：核心判断结果，`yes` 表示这份报告确实是该指标的检测报告，进入第二步解读
- `status`：细化状态，如"已出结果/检测中/仅病史提及/未检测"，用于过滤"提及了但没有本次结果"的情况
- `evidence`：引用原文关键片段，便于人工复核，也降低模型瞎判的概率

### Prompt要点

- 强调"判断的是报告类型，不是关键词是否出现"
- 明确排除既往病史/背景信息的干扰
- 要求输出严格JSON（建议用structured output / function calling强约束格式）

---

## 五、第二步：仅对"是"的指标进行解读

### 依赖关系

**第二步的执行范围完全由第一步的`is_report_type=yes`结果决定**——只有第一步判定为"是"的指标才会进入本步骤，其余指标不调用LLM、不产生解读，直接跳过。

### 输入

- 第一步判定为yes的指标列表
- 对应的原文片段（`evidence`可直接复用，或按需重新提取更完整的上下文）
- 对应指标的`key_fields`（来自配置文件）

### 输出格式（结构化，而非纯自然语言）

建议一次调用批量输出全部有效指标的解读（而非逐个循环调用），既减少调用次数，又能让模型在有多个指标同时存在时给出联合解读（如MRD阴性+HER2阳性的治疗提示）：

```json
{
  "MRD": {
    "summary": "结论性解读，约150字以内",
    "key_values": {"MRD状态": "阳性", "VAF": "0.05%", "采样时间点": "术后4周"},
    "clinical_significance": "临床意义说明",
    "confidence": "high",
    "source_quote": "引用原文，用于溯源，禁止编造数值"
  },
  "HER2": {
    "summary": "...",
    "key_values": {"IHC结果": "3+", "FISH结果": "未检"},
    "clinical_significance": "...",
    "confidence": "medium",
    "source_quote": "..."
  }
}
```

### Prompt要点

- 明确"仅基于给定原文解读，不得引入文本外的医学假设或编造数值"
- 每个指标必须包含`source_quote`溯源，防止幻觉
- 增加`confidence`字段标注解读把握度，低置信度需人工复核
- 统一附加免责声明：`本解读仅供参考，具体诊疗请以医生意见为准`
- 总字数控制建议按指标数量分配（如每指标150~200字），而非笼统"500字左右"

---

## 六、整体流程图（文字版）

```
PDF文件
  │
  ▼
提取前N页文本（默认前3~5页，未命中则兜底扩展）
  │
  ▼
【LLM调用1】报告类型判断
  输入：关键页文本 + 配置文件（含aliases）
  输出：{指标: {is_report_type, status, evidence}}
  │
  ▼
过滤 is_report_type == "yes" 的指标
  │
  ├── 若全部为no → 结束，输出"本报告不包含配置指标，无需解读"
  │
  ▼
【LLM调用2】批量专项解读（仅针对yes指标）
  输入：yes指标对应原文 + key_fields
  输出：{指标: {summary, key_values, clinical_significance, confidence, source_quote}}
  │
  ▼
渲染最终解读报告
```

---

## 七、工程实现补充建议

1. **模型分工**：第一步是分类任务，可用轻量/低成本模型；第二步是生成任务，建议用能力更强的模型
2. **异常与重试**：LLM返回非法JSON时重试1~2次，仍失败则标记人工处理；PDF提取为空页时同理
3. **一致性兜底**：第二步执行时如发现某指标其实缺少有效数值（第一步误判），应允许返回"数据不足，无法解读"而非强行编造
4. **可追溯性**：保留每次调用的输入文本片段、prompt、模型原始输出，便于医疗场景审计
5. **表格提取质量**：TPS/IHC分级等结果常以表格形式呈现，建议评估是否需要专门的表格结构化提取，避免纯文本提取导致数值错位

---

## 八、后续可细化方向

- [ ] 具体页数/字符数阈值需要用真实样本报告测试确定
- [ ] 第一步、第二步的具体prompt文案（可在此基础上进一步撰写）
- [ ] 兜底扩展提取的触发条件与页数上限
- [ ] 多指标联合解读时的优先级与篇幅分配规则
